import { Grid } from "@mui/material";
import React from "react";
import styles from "./Footer.module.css";

const Footer = () => {
  return <Grid container className={styles.container}></Grid>;
};

export default Footer;
